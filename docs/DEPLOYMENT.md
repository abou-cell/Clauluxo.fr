# Déploiement

La Phase 1 est indépendante de Firebase. Les commandes prévues sont flutter pub get, flutter analyze, flutter test, flutter build web --release et flutter build apk --release, sur un environnement Flutter configuré.
